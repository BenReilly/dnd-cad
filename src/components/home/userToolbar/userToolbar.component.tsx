import { Button } from '@mui/material';

const UserToolbar = () => {
  return (
    <div className="dndcad-user-toolbar">
      <Button variant="text" onClick={() => {}}>
        Generate
      </Button>
    </div>
  );
};

export default UserToolbar;
